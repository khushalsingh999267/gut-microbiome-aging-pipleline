# config.yaml
project: PRJNA931847
srr_list_file: metadata/srr_ids.txt   # generated by scripts/get_srrs.sh
raw_dir: data/raw
threads: 2      # <=2 threads so we never exceed ~1 GB
mem_mb: 1000    # cap fasterq-dump buffers at ~1 GB
